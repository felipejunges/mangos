@model IEnumerable<Mangos.Mvc.Models.ViewModels.ContaBancariaListaModel>

<div class="table-responsive">
    <table class="table table-striped table-hover table-sm">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descricao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumeroBanco)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Agencia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumeroConta)
            </th>
            <th class="text-center">
                @Html.DisplayNameFor(model => model.Ativo)
            </th>
            <th class="text-right">
                @Html.DisplayNameFor(model => model.SaldoInicial)
            </th>
            <th></th>
        </tr>

        @if (Model.Count() == 0)
        {
            <tr>
                <td colspan="7" class="text-center">Nenhum resultado encontrado</td>
            </tr>
        }
        else
        {
            foreach (var item in Model)
            {
                <tr class="@(!item.Ativo ? "table-item-inativo" : "")">
                    <td>
                        @Html.DisplayFor(modelItem => item.Descricao)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.NumeroBanco)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Agencia)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.NumeroConta)
                    </td>
                    <td class="text-center">
                        @Html.DisplayFor(modelItem => item.Ativo)
                    </td>
                    <td class="text-right">
                        @Html.DisplayFor(modelItem => item.SaldoInicial)
                    </td>
                    <td class="text-right">
                        <open-modal-icon controller="ContaBancaria" action="Dados" value="@item.Id.ToString()" glyph-icon="edit"></open-modal-icon>
                        <excluir-icon controller="ContaBancaria" value="@item.Id.ToString()"></excluir-icon>
                    </td>
                </tr>
            }
        }

    </table>
</div>
